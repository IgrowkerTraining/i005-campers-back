name: Backend

on:
  push:
    paths:
      - 'i005-campers-back/**'
    branches:
      - main
  workflow_dispatch:

jobs:
  build-push-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 22.14.0

      - name: Install dependencies
        run: |
          npm install

      - name: Build NestJS project
        run: |
          npm run build

      - name: Authenticate with Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY_BK }}

      - name: Configure Docker to use Artifact Registry
        run: |
          gcloud auth configure-docker ${{ secrets.ARTIFACT_REGISTRY_REGION }}-docker.pkg.dev

      - name: Docker build image
        run: |
          docker build -t ${{ secrets.ARTIFACT_REGISTRY_URL }}/campers-back${TAG::7} .

      - name: Push Docker image to Artifact Registry
        run: |
          docker push ${{ secrets.ARTIFACT_REGISTRY_URL }}

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy i005-campers-back \
            --image ${{ secrets.ARTIFACT_REGISTRY_URL }} \
            --region ${{ secrets.GCP_REGION }} \
            --platform managed \
            --allow-unauthenticated


